# 📊 行业龙头与热点分析 - 使用指南

## 🎯 功能概述

**行业龙头与热点分析系统**是一个强大的市场分析工具，能够：

1. **实时追踪热点** 🔥 - 分析当天A股、港股、美股的热门行业
2. **筛选龙头公司** 🏆 - 找出各行业在不同市场的领先企业
3. **深度数据分析** 📊 - 提供市值、营收、市场份额等关键指标
4. **智能推荐** 💡 - 基于AI的行业趋势和投资机会分析

---

## 🚀 快速开始

### 方式1：命令行界面（CLI）

最简单的使用方式，适合快速查询：

```bash
python sector_hotspot_cli.py
```

**功能菜单**：
1. 查看今日热点行业
2. 查找特定行业龙头
3. 生成完整分析报告
4. 查看行业列表
5. 退出

### 方式2：Web界面（推荐）

更直观的可视化界面：

```bash
python sector_hotspot_web.py
```

然后在浏览器打开：`http://localhost:7861`

**Web界面特点**：
- 🎨 美观的可视化设计
- 📊 实时交互式分析
- 💾 一键下载报告
- 📱 支持移动端

### 方式3：Python代码调用

集成到你自己的项目中：

```python
from agents.sector_leader_analyzer import SectorLeaderAnalyzer

analyzer = SectorLeaderAnalyzer()

# 方法1: 分析今日热点
hotspots = analyzer.analyze_market_hotspots()
print(hotspots)

# 方法2: 查找行业龙头
leaders = analyzer.find_sector_leaders("科技", ["A-share", "HK", "US"])
print(leaders)

# 方法3: 生成完整报告
report = analyzer.generate_hotspot_report()
print(report["report"])
```

---

## 📖 详细功能说明

### 1. 今日热点分析 🔥

**功能**：实时分析三大市场的热门行业板块

**CLI使用**：
```bash
# 启动CLI
python sector_hotspot_cli.py

# 选择功能 1
```

**Python使用**：
```python
from agents.sector_leader_analyzer import SectorLeaderAnalyzer

analyzer = SectorLeaderAnalyzer()
result = analyzer.analyze_market_hotspots()

# 结果包含
if result["status"] == "success":
    for sector in result["top_sectors"]:
        print(f"行业: {sector['sector']}")
        print(f"市场: {sector['market']}")
        print(f"热度: {sector['heat_score']}")
        print(f"涨跌幅: {sector['avg_change']}")
        print(f"成交量变化: {sector['volume_surge']}")
        print(f"驱动因素: {sector['key_drivers']}")
        print()
```

**输出示例**：
```
排名  行业              市场      热度    涨跌幅      成交量
1     半导体            A-share   🔥92    +5.2%      +180%
      💡 驱动因素: AI芯片需求, 国产替代
      🏆 热门股票: 中芯国际, 北方华创, 长电科技

2     人工智能          US        ⭐85    +3.8%      +145%
      💡 驱动因素: OpenAI新品发布, 企业AI采用率提升
      🏆 热门股票: NVIDIA, Microsoft, Google
```

### 2. 行业龙头筛选 🏆

**功能**：查找指定行业在各市场的领先公司

**CLI使用**：
```bash
python sector_hotspot_cli.py
# 选择功能 2
# 输入行业: 科技
# 选择市场: 4 (全部市场)
```

**Python使用**：
```python
analyzer = SectorLeaderAnalyzer()

# 查找科技行业龙头
result = analyzer.find_sector_leaders(
    sector="科技",
    markets=["A-share", "HK", "US"]
)

# 遍历结果
for market, companies in result["markets"].items():
    print(f"\n{market} 市场:")
    for company in companies:
        print(f"  {company['rank']}. {company['company']} ({company['ticker']})")
        print(f"     市值: {company['market_cap']}")
        print(f"     营收: {company['key_metrics']['revenue']}")
        print(f"     优势: {company['competitive_advantages']}")
```

**输出示例**：
```
🏆 科技 行业龙头公司

================================================================================
  A-share 市场
================================================================================

🏆 1. 腾讯控股 (00700.HK)
   💰 市值: $478.5B
   📈 营收: $86.2B
   📊 市场份额: 23%
   📈 增长率: +12%
   ⭐ 优势: 社交媒体垄断, 游戏业务领先, 云计算快速增长
   📝 近期表现: Q3业绩超预期，游戏收入同比增长32%

🏆 2. 阿里巴巴 (9988.HK)
   💰 市值: $234.8B
   📈 营收: $126.5B
   📊 市场份额: 18%
   📈 增长率: +8%
   ⭐ 优势: 电商生态系统, 云计算基础设施, 物流网络
```

### 3. 完整分析报告 📊

**功能**：生成包含热点分析和龙头公司的完整Markdown报告

**CLI使用**：
```bash
python sector_hotspot_cli.py
# 选择功能 3
# 等待2-5分钟
# 报告自动保存到 reports/ 目录
```

**Python使用**：
```python
analyzer = SectorLeaderAnalyzer()

# 生成完整报告
result = analyzer.generate_hotspot_report()

if result["status"] == "success":
    # 报告内容
    print(result["report"])
    
    # 保存到文件
    with open("my_sector_report.md", "w", encoding="utf-8") as f:
        f.write(result["report"])
```

**报告包含**：
- 📅 今日市场概览
- 🔥 热点行业排行榜
- 🏆 各行业龙头公司详情
- 📊 竞争格局分析
- 💡 市场洞察和建议

---

## 📚 支持的行业板块

系统预定义了9大行业板块及其子行业：

| 行业 | 中文名 | 子行业 |
|------|--------|--------|
| Technology | 科技 | 半导体、软件、互联网、人工智能、云计算 |
| Finance | 金融 | 银行、保险、证券、支付 |
| Healthcare | 医疗健康 | 医药、医疗器械、生物科技、医疗服务 |
| Consumer | 消费 | 零售、食品饮料、家电、汽车 |
| Energy | 能源 | 石油天然气、新能源、电力、煤炭 |
| Industrial | 工业 | 制造、建筑、机械、运输 |
| Real Estate | 房地产 | 房地产开发、物业管理、REITS |
| Materials | 材料 | 化工、金属、矿业、建材 |
| Telecom | 通信 | 电信运营、通信设备、5G |

**查看完整列表**：
```bash
python sector_hotspot_cli.py
# 选择功能 4
```

---

## 🎨 Web界面功能详解

### 启动Web界面

```bash
python sector_hotspot_web.py
```

访问：`http://localhost:7861`

### 界面功能

#### Tab 1: 今日热点 🔥
- 点击"分析今日热点"按钮
- 查看热度排行榜
- 彩色热度指标（红色=火热，黄色=热门，绿色=活跃）
- 查看各板块热门股票

#### Tab 2: 行业龙头 🏆
- 输入行业名称（中文或英文）
- 选择市场（A股/港股/美股/全部）
- 点击"查找龙头"
- 查看详细公司信息卡片

#### Tab 3: 完整报告 📊
- 点击"生成完整报告"
- 等待2-5分钟
- 查看报告内容
- 下载Markdown文件

#### Tab 4: 使用说明 📖
- 功能介绍
- 热度评分说明
- 数据来源
- 免责声明

---

## 💡 使用场景示例

### 场景1：每日市场监控

**需求**：每天早上快速了解市场热点

**方案**：
```python
# 创建每日定时任务
from agents.sector_leader_analyzer import SectorLeaderAnalyzer
from datetime import datetime

def daily_market_monitor():
    analyzer = SectorLeaderAnalyzer()
    
    # 分析热点
    hotspots = analyzer.analyze_market_hotspots()
    
    # 提取TOP 3热点
    top_3 = hotspots["top_sectors"][:3]
    
    # 发送通知或邮件
    summary = f"今日热点 ({datetime.now().date()}):\n"
    for i, sector in enumerate(top_3, 1):
        summary += f"{i}. {sector['sector']} ({sector['market']}) "
        summary += f"热度:{sector['heat_score']} 涨幅:{sector['avg_change']}\n"
    
    print(summary)
    return summary

# 运行
daily_market_monitor()
```

### 场景2：行业研究

**需求**：深入研究某个行业的竞争格局

**方案**：
```bash
# 使用CLI
python sector_hotspot_cli.py
# 选择功能 2
# 输入: 半导体
# 选择: 全部市场

# 得到A股、港股、美股的半导体龙头对比
```

### 场景3：投资组合构建

**需求**：选择不同市场的优质龙头进行配置

**方案**：
```python
analyzer = SectorLeaderAnalyzer()

# 查找多个行业的龙头
sectors = ["科技", "金融", "医疗健康", "消费"]
portfolio = {}

for sector in sectors:
    leaders = analyzer.find_sector_leaders(sector, ["A-share", "HK", "US"])
    
    # 提取每个市场的TOP 1
    for market, companies in leaders["markets"].items():
        if companies:
            top_company = companies[0]
            portfolio[f"{sector}_{market}"] = {
                "name": top_company["company"],
                "ticker": top_company["ticker"],
                "market_cap": top_company["market_cap"]
            }

# 输出投资组合
for key, info in portfolio.items():
    print(f"{key}: {info['name']} ({info['ticker']}) - {info['market_cap']}")
```

### 场景4：周报/月报生成

**需求**：定期生成行业分析报告

**方案**：
```python
from agents.sector_leader_analyzer import SectorLeaderAnalyzer
from datetime import datetime
import schedule
import time

def generate_weekly_report():
    analyzer = SectorLeaderAnalyzer()
    result = analyzer.generate_hotspot_report()
    
    if result["status"] == "success":
        filename = f"reports/weekly_report_{datetime.now().strftime('%Y%m%d')}.md"
        with open(filename, "w", encoding="utf-8") as f:
            f.write(result["report"])
        print(f"周报已生成: {filename}")

# 每周一早上9点生成报告
schedule.every().monday.at("09:00").do(generate_weekly_report)

while True:
    schedule.run_pending()
    time.sleep(60)
```

---

## 🔧 高级配置

### 自定义行业分类

编辑 `agents/sector_leader_analyzer.py`：

```python
# 在 __init__ 方法中添加自定义行业
self.sectors["my_sector"] = {
    "name_cn": "自定义行业",
    "name_en": "Custom Sector",
    "subsectors": ["子行业1", "子行业2"]
}
```

### 调整热度评分权重

```python
# 修改热度评分计算逻辑
# 在 _analyze_hotspots_with_ai 方法的 user_prompt 中：

"""
Heat score calculation: 
- Price change weight: 40%     # 原30%，提高价格权重
- Volume surge weight: 30%     # 保持不变
- News catalyst weight: 20%    # 原25%，降低新闻权重
- Money flow weight: 10%       # 原15%，降低资金流权重
"""
```

### 修改查询数量

```python
# 在 find_sector_leaders 方法中
leaders = self._find_market_leaders(sector, market)

# 在 _extract_leaders_with_ai 中修改
"Focus on TOP 5 companies only."  # 改为TOP 10
```

---

## 📊 热度评分算法说明

### 计算公式

```
热度分数 = 价格变动(30%) + 成交量激增(30%) + 新闻催化(25%) + 资金流向(15%)
```

### 各项指标说明

1. **价格变动** (30%)
   - 当日涨跌幅
   - 与行业平均对比
   - 持续性评估

2. **成交量激增** (30%)
   - 成交量同比变化
   - 换手率
   - 资金活跃度

3. **新闻催化** (25%)
   - 重大新闻数量
   - 政策利好
   - 行业事件影响

4. **资金流向** (15%)
   - 主力资金净流入
   - 机构持仓变化
   - 北向资金动向

### 评级标准

| 分数 | 等级 | 图标 | 说明 |
|------|------|------|------|
| 90-100 | 极度火热 | 🔥 | 市场关注度极高，建议重点关注 |
| 70-89 | 热门板块 | ⭐ | 表现强劲，值得配置 |
| 50-69 | 活跃板块 | 📊 | 有一定活跃度，可适度关注 |
| 30-49 | 一般活跃 | - | 正常波动，观望为主 |
| 0-29 | 相对冷门 | - | 关注度较低，谨慎参与 |

---

## 🎯 最佳实践

### 1. 数据时效性

- **最佳查询时间**：交易日收盘后1小时
- **热点分析**：建议每日更新
- **龙头筛选**：可每周更新

### 2. 结果验证

```python
# 交叉验证不同来源
result1 = analyzer.analyze_market_hotspots()
# 等待1小时后再次查询
result2 = analyzer.analyze_market_hotspots()

# 对比结果
for s1, s2 in zip(result1["top_sectors"], result2["top_sectors"]):
    if s1["sector"] == s2["sector"]:
        print(f"{s1['sector']}: 热度从 {s1['heat_score']} 变为 {s2['heat_score']}")
```

### 3. 组合使用

```python
# 结合估值报告系统
from main import ValuationReportSystem
from agents.sector_leader_analyzer import SectorLeaderAnalyzer

# 1. 找到热点行业
analyzer = SectorLeaderAnalyzer()
hotspots = analyzer.analyze_market_hotspots()
top_sector = hotspots["top_sectors"][0]

# 2. 找到该行业龙头
leaders = analyzer.find_sector_leaders(top_sector["sector"], ["US"])
top_company = leaders["markets"]["US"][0]["company"]

# 3. 生成该龙头的深度估值报告
valuation_system = ValuationReportSystem()
valuation_report = valuation_system.generate_report(company=top_company)

print(f"✅ 已为热点行业 {top_sector['sector']} 的龙头 {top_company} 生成深度报告")
```

---

## ❓ 常见问题

### Q1: 支持哪些市场？
**A**: 目前支持A股、港股、美股三大市场。未来可扩展到其他市场。

### Q2: 数据更新频率？
**A**: 实时查询，每次调用都获取最新数据。建议交易日收盘后查询以获得完整数据。

### Q3: 可以查询历史热点吗？
**A**: 当前版本focus于当日数据。历史数据功能在开发中。

### Q4: 热度评分准确吗？
**A**: 评分基于多个客观指标和AI分析，有一定参考价值，但应结合其他分析工具综合判断。

### Q5: 可以自定义行业吗？
**A**: 可以！参考"高级配置"章节修改代码添加自定义行业。

### Q6: Web界面可以远程访问吗？
**A**: 默认只监听本地。如需远程访问，修改 `server_name="0.0.0.0"` 并配置防火墙。

### Q7: 数据来源是什么？
**A**: 
- 实时市场数据：Perplexity Sonar API
- AI分析：Qwen3-Max
- 多源交叉验证确保准确性

### Q8: 报告可以定时生成吗？
**A**: 可以！参考"场景4：周报/月报生成"使用schedule库实现定时任务。

---

## 🆘 故障排除

### 问题1: 热点分析返回空结果

**原因**：可能是市场休市或数据源暂时不可用

**解决**：
```python
# 添加重试逻辑
import time

max_retries = 3
for i in range(max_retries):
    result = analyzer.analyze_market_hotspots()
    if result.get("top_sectors"):
        break
    print(f"重试 {i+1}/{max_retries}...")
    time.sleep(5)
```

### 问题2: 龙头查询很慢

**原因**：查询多个市场需要更多时间

**解决**：
```python
# 只查询单个市场
leaders = analyzer.find_sector_leaders("科技", ["US"])  # 只查美股

# 或使用异步（高级）
import asyncio

async def query_markets():
    tasks = [
        analyzer.find_sector_leaders("科技", ["A-share"]),
        analyzer.find_sector_leaders("科技", ["HK"]),
        analyzer.find_sector_leaders("科技", ["US"])
    ]
    results = await asyncio.gather(*tasks)
    return results
```

### 问题3: Web界面端口被占用

**解决**：
```python
# 修改 sector_hotspot_web.py
app.launch(server_port=7862)  # 改用其他端口
```

### 问题4: 中文行业名识别不准

**解决**：
```python
# 同时提供中英文
leaders = analyzer.find_sector_leaders("科技 Technology", markets)
```

---

## 📞 获取支持

- 📧 问题反馈：提交Issue
- 📖 文档：查看本指南
- 💡 建议：欢迎提交功能建议

---

## 🎉 开始使用

现在你已经了解了所有功能，选择一种方式开始：

```bash
# 方式1: CLI
python sector_hotspot_cli.py

# 方式2: Web界面
python sector_hotspot_web.py

# 方式3: Python代码
python
>>> from agents.sector_leader_analyzer import SectorLeaderAnalyzer
>>> analyzer = SectorLeaderAnalyzer()
>>> result = analyzer.analyze_market_hotspots()
```

**祝你使用愉快！** 📊🚀

---

**文档版本**: v1.0  
**最后更新**: 2025-11-05  
**系统版本**: v1.0

